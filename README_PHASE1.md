# PT Clinical Operating System - Phase 1 Refactor

Copy these folders into your project root:

- app
- components
- data
- lib
- types
- utils

Then run:

npm run build
git add .
git commit -m "Phase 1 architecture refactor"
git push

This refactor separates the previous single-file page.tsx into scalable modules.
